{% block body %}
<form action="">
    <input type="text" name="term" value="{{ term|striptags }}">
    <input type="submit" value="Find user">
</form>
<a href="{{ path_for('addUser') }}">Регистрация</a>
<ul>
{{ message }}
    {% for user in users %}
        <li>{{ user['name']}} - {{ user['email']}}</li>
    {% endfor %}
<li><a href='{{ routname }}'>course - 200</a></li>
</ul>
{% endblock %}